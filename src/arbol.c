#include "arbol.h"

// puntero al nodo mas pequeño 
tree_node_t *smallest_nice;

// Inicializa el arbol y su piscina de elementos
void init_arbol(tree_t *_tree, int _maxElem){

    // inicializa la queue/pool y pilla un solo elemento para ponerlo como root?

}

// funcion de introducir nodo
void addNode(tree_t *_tree, pcb_t *_elem){
    // Pilla nodo, mete elemento y busca ordenadamente

    /* casos:
        primero
        mayor
        menor
        igual
    */
}

// funcion de sacar nodo
pcb_t* removeNode(tree_node_t *_toRemove, tree_t _tree){

}